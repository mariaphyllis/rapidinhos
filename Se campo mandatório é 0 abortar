(function executeRule(current, previous /*null when async*/) {
	
	gs.addErrorMessage(gs.getMessage('Mensagem_UI'));
	current.setAbortAction(true);

})(current, previous);

// Se campo mandatório é R$0 abortar e escrever mensagem
